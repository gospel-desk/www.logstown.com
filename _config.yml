url: https://www.gospeldesk.org
sass:
  sass_dir: .
exclude: ["CNAME", "Makefile", "README.md"]
defaults:
  -
    scope:
      path: ""
    values:
      layout: "page"
      html_title_sep: "-"
      show_subscribe: "yes"
collections:
  posts:
    permalink: /updates/:year/:title/
